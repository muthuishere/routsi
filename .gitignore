# build output
/bin/
*.exe

# local config that may hold real endpoints/keys-by-name for a machine
/models.local.yaml

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# dashboard build (ui/ -> internal/server/public is COMMITTED so `go build`
# works without a node toolchain; only the toolchain's own scratch is ignored)
/ui/node_modules/
/ui/tsconfig.tsbuildinfo

# go
*.test
*.out
coverage.*
